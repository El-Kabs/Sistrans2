Instrucciones

1) instalar JBoss
2) instalar WildFly
3) buscar en la carpeta de WildFly la carpeta standAlone
4) buscar en la carpeta standAlone la carpeta deployments
5) poner el archivo war en esta carpeta
6) ir de nuevo a la carpeta de wildFly
7) buscar la carpeta bin
8) buscar el archivo standalone.bat
9) utilizar postMan para probar los servicios